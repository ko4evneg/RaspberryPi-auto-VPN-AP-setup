# HOW-TO
1. Delete C:\Users\Ko4evneG\.ssh\known_hosts
2. Connect to RPi: `ssh rpi.local`
3. Copy script: `nano setup.sh`
4. Set script permissions: `chmod +x setup.sh`
5. Run script: `./setup.sh 'WiFi_AP_password'`
